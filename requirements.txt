pydantic>=2.6
requests>=2.31
bittensor
# Pins align with scripts/miner/setup.sh and bittensor SDK (bittensor 10.x pins async-substrate-interface==2.0.2).
async-substrate-interface==2.0.2
bittensor-cli==9.20.0
bittensor-wallet==4.0.1
# bittensor-cli may pull scalecodec; it conflicts with cyscale (async-substrate-interface). If btcli errors, run:
#   pip uninstall scalecodec -y && pip install cyscale --force-reinstall
python-dotenv>=1.0
numpy>=1.24
scikit-learn>=1.3
pandas>=2.3.3
wandb>=0.19
